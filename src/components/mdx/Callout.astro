---
interface Props {
    type?: "info" | "warning" | "error" | "success" | "tip";
    title?: string;
}

const { type = "info", title } = Astro.props;

const colors: Record<string, string> = {
    info: "bg-blue-50 border-blue-200 text-blue-900",
    warning: "bg-amber-50 border-amber-200 text-amber-900",
    error: "bg-red-50 border-red-200 text-red-900",
    success: "bg-emerald-50 border-emerald-200 text-emerald-900",
    tip: "bg-purple-50 border-purple-200 text-purple-900",
};

const icons: Record<string, string> = {
    info: "ℹ️",
    warning: "⚠️",
    error: "🚫",
    success: "✅",
    tip: "💡",
};

const style = colors[type] || colors.info;
const icon = icons[type] || icons.info;
---

<div class={`my-8 p-6 rounded-lg border-l-4 ${style} not-prose`}>
    <div class="flex gap-4 items-start">
        <span class="text-2xl select-none" role="img" aria-hidden="true"
            >{icon}</span
        >
        <div class="prose prose-sm max-w-none">
            {title && <p class="font-bold mb-1 mt-0">{title}</p>}
            <slot />
        </div>
    </div>
</div>
